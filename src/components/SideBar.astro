---
import SideBarFooter from "./SideBarFooter.astro";
import { Image } from "astro:assets";
import SideBarMenu from "./SideBarMenu.astro";


const { sideBarActiveItemID } = Astro.props;
---

<div class="drawer-side z-40">
  <label for="my-drawer" class="drawer-overlay"></label>
  <aside class="px-2 pt-2 h-auto min-h-full w-[19rem] bg-base-200 text-base-content flex flex-col">
    <div class="w-fit mx-auto mt-5 mb-6">
      <!-- <a href="/">
        <div class="avatar transition ease-in-out hover:scale-[102%] block m-auto">
          <div class="w-[8.5rem]">
            <Image class="mask mask-circle" format="webp" width={300} height={300} src="/profile.webp" alt="Profile image" />
          </div>
        </div>
      </a> -->
     <div class="inline-block"> <!-- Use inline-block to place elements on the same line -->
     <a href="/">
        <h1 class="gear" style="display: inline;">⚙</h1> <!-- Set display to inline -->
        <h1 class="redTitle" style="display: inline;">GALILEO</h1> <!-- Set display to inline -->
        <h1 class="purpleTitle" style="display: inline;">DYNASIM</h1> <!-- Set display to inline -->
        </a>
      </div>
    </div>
    <SideBarMenu sideBarActiveItemID={sideBarActiveItemID} />
    <SideBarFooter />
  </aside>
</div>

<style>

   .gear {
    color: red; /* Set the color to red */
    font-size: xx-large;

  }
  .redTitle {
    color: red; /* Set the color to red */
    font-size: x-large;
    font-weight: bolder;
  }

  .purpleTitle {
    color: purple; /* Set the color to purple */
    font-size: x-large;
    font-weight: bolder;
  }
</style>